
%ul
  %li{ class: jstree_segment_class(segment), "segment-id" => segment.id, id: segment.id }
    = link_to segment.name, segment_path(segment)
    - if is_segment_open?(segment)
      = render segment.children
