#left-sidebar-content.d-flex.flex-column
  = left_sidebar_gravatar_link(t(:profile), edit_profile_path(current_user), wrapper_options: { class: ""})

  = left_sidebar_link(t(:dashboard), root_path, "dashboard")
  = left_sidebar_link("Reportes", reports_path, "book")
  = #left_sidebar_link(t(:catalogs), procedure_catalogs_path , "book")
  - if root_segment
    - if can? :manage, root_segment
      = left_sidebar_link("Distritos", root_segments_route, "sitemap")
    = left_sidebar_link(t(:capture), prep_capture_process_route, "pencil")

  .mt-auto.d-flex.flex-column
    = left_sidebar_link(t(:sign_out), logout_path(current_user), "sign-out", wrapper_options: { class: "sidebar-bottom-link" }, method: :delete)
